graph(%1 : Float(*, *)
      %2 : Float(*)
      %3 : Float(*)) {
  %4 : Float(*, *) = prim::FusionGroup_0[device=0](%3, %1, %2)
  return (%4);
}
with prim::FusionGroup_0 = graph(%1 : Float(*)
      %2 : Float(*, *)
      %3 : Float(*)) {
  %4 : Float(*, *) = aten::mul(%2, %3)
  %5 : int = prim::Constant[value=1]()
  %6 : Float(*, *) = aten::add(%4, %1, %5)
  return (%6);
}
