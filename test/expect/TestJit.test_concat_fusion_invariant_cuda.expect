graph(%1 : Float(*, *)
      %2 : Float(*, *)
      %3 : Float(*, *)) {
  %4 : int = prim::Constant[value=1]()
  %5 : Float(*, *) = prim::FusionGroup_0[device=0](%1, %2)
  %6 : Float(*, *) = aten::add(%5, %3, %4)
  return (%6);
}
with prim::FusionGroup_0 = graph(%1 : Float(*, *)
      %2 : Float(*, *)) {
  %3 : int = prim::Constant[value=1]()
  %4 : Float(*, *) = aten::add(%1, %2, %3)
  %5 : int = prim::Constant[value=1]()
  %6 : Float(*, *) = aten::sub(%1, %2, %5)
  %7 : Float(*, *) = prim::FusedConcat[dim=0](%4, %6)
  return (%7);
}
