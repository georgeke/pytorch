graph(%1 : Float(*, *)
      %2 : Float(*, *)) {
  %3 : Float(*, *) = prim::FusionGroup_0[device=0](%1, %2)
  return (%3);
}
with prim::FusionGroup_0 = graph(%1 : Float(*, *)
      %2 : Float(*, *)) {
  %3 : int = prim::Constant[value=1]()
  %4 : Float(*, *) = aten::add(%1, %2, %3)
  %5 : Float(*, *) = aten::mul(%1, %2)
  %6 : Float(*, *) = prim::FusedConcat[dim=0](%4, %5)
  return (%6);
}
