graph(%1 : Float(*)
      %2 : Float(*)) {
  %3 : Float(*) = prim::FusionGroup_0[device=1](%1, %2)
  return (%3);
}
with prim::FusionGroup_0 = graph(%1 : Float(*)
      %2 : Float(*)) {
  %3 : int = prim::Constant[value=1]()
  %4 : Float(*) = aten::add(%1, %2, %3)
  %5 : Float(*) = aten::mul(%1, %4)
  %6 : int = prim::Constant[value=1]()
  %7 : Float(*) = aten::add(%5, %1, %6)
  %8 : Float(*) = aten::tanh(%7)
  %9 : Float(*) = aten::sigmoid(%8)
  return (%9);
}
