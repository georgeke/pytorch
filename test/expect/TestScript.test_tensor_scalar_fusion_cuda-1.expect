graph(%x : Float(*, *)) {
  %2 : Float(*, *) = prim::FusionGroup_0[device=0](%x)
  return (%2);
}
with prim::FusionGroup_0 = graph(%1 : Float(*, *)) {
  %z : float = prim::Constant[value=3]()
  %3 : int = prim::Constant[value=1]()
  %y : Float(*, *) = aten::add(%1, %z, %3)
  %5 : Float(*, *) = aten::mul(%1, %y)
  return (%5);
}
