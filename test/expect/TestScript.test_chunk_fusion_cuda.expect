graph(%x : Float(*, *)) {
  %2 : Float(*, *) = prim::FusionGroup_0[device=0](%x)
  return (%2);
}
with prim::FusionGroup_0 = graph(%1 : Float(*, *)) {
  %2 : Float(*, *), %3 : Float(*, *), %4 : Float(*, *) = prim::ConstantChunk[chunks=3, dim=1](%1)
  %5 : Float(*, *) = aten::mul(%2, %3)
  %6 : int = prim::Constant[value=1]()
  %7 : Float(*, *) = aten::add(%5, %4, %6)
  return (%7);
}
